{
  "version": 2,
  "builds": [
    { "src": "dist/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/auth/(.*)", "dest": "/api/auth/$1" },
    { "src": "/api/(.*)", "dest": "dist/index.js" }
  ]
}