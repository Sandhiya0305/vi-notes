{
  "version": 2,
  "buildCommand": "cd .. && npm run build --prefix=client",
  "outputDirectory": "client/dist",
  "installCommand": "cd .. && npm install --prefix=client",
  "framework": null,
  "cleanUrls": true,
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}